apiVersion: v1
kind: Service
metadata:
  name: {{ include "fulfillment-service.fullname" . }}
  labels:
    {{- include "fulfillment-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fulfillment-service.selectorLabels" . | nindent 4 }}
