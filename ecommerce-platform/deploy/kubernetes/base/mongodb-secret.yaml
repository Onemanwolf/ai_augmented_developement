apiVersion: v1
kind: Secret
metadata:
  name: mongodb-credentials
  labels:
    app.kubernetes.io/part-of: ecommerce-platform
type: Opaque
stringData:
  # These should be overridden per environment using Kustomize secretGenerator
  # or a secrets management solution like Sealed Secrets / External Secrets
  uri: "mongodb://mongodb:27017"
