apiVersion: v1
kind: Service
metadata:
  name: fulfillment-service
  labels:
    app: fulfillment-service
    app.kubernetes.io/name: fulfillment-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: fulfillment-service
